export EDITOR=nvim
export IDF_TOOLS_PATH="$XDG_DATA_HOME/esp-idf"
# Alias for managing dotfiles via git
alias dots='/usr/bin/git --git-dir=$XDG_STATE_HOME/dots --work-tree=$HOME'
# Alias for lazyvim
alias lazyvim='NVIM_APPNAME=lazyvim nvim'
# Alias for esp-idf
alias esp-idf='source /opt/esp-idf/export.sh'

# Additional Keybindings
bindkey '^H' slash-backward-kill-word
